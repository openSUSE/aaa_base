setup
=====

This repository makes up the tar ball in the setup package
https://build.opensuse.org/package/show/Base:System/setup

After commit, checkout Base:System setup and run
  osc service mr

To build setup locally directly from the git checkout run
  rpmbuild --build-in-place --noprep setup.spec -bb
